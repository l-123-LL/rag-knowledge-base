# AI Agent 应用开发社招面试材料与学习路线

适用项目：企业智能客服 RAG 的最小改动升级版

适用背景：半年 AI 工作经验，目标岗位为 AI 应用开发、LLM 应用、RAG 或 Agent 开发

重要说明：本文中的 `[X]`、`[Y]` 必须替换为真实运行结果。没有真实数据时，不要写入简历，
也不要在面试中声称已经达到。

## 一、先明确项目定位

不要把这个项目讲成“我做了一个全新 Agent 平台”。

最准确、可信的定位是：

> 我在原有企业客服 RAG 基础上，增加了一个可选的工具调用工作流，让系统可以在知识问答、
> 订单查询和人工转接之间做路由，并对工具调用、重试、耗时、Token 和成本进行追踪和评测。

项目升级的核心目标：

1. 保持原有 RAG 模式稳定。
2. 新增 `workflow_mode="tools"`。
3. 增加三个必要工具。
4. 增加最小状态机。
5. 增加 Trace。
6. 把评测从 50 条扩充到 100 条。
7. 形成一个可以现场演示的完整场景。

## 二、30 秒自我介绍

> 我有半年 AI 应用开发经验，主要做企业知识库问答和 LLM 应用工程。最近在企业客服 RAG
> 项目上增加了一个可选的工具调用工作流：普通问题继续走原有 RAG，订单类问题调用订单工具，
> 低相关或工具连续失败时转人工。整个流程有状态、超时、重试和执行 Trace，也建立了
> 100 条任务的评测集，持续跟踪任务成功率、工具选择、引用准确率、延迟和成本。

## 三、2 分钟项目介绍

> 这个项目原来的定位是企业客服 RAG，用户上传产品手册、FAQ、售后政策等资料后，系统可以做
> 混合检索、流式回答和引用溯源。
>
> 但它最初更接近一个 RAG 问答系统，缺少工具调用和失败恢复，所以我做了一次最小范围升级。
> 我没有重写原来的检索链路，而是在入口增加了一个可选的 `tools` 模式。这个模式先做意图
> 路由：知识问题调用现有的知识检索工具，订单问题调用本地订单工具，需要人工处理时创建
> 工单并转人工。
>
> 工作流限制了最多三次工具调用，同一工具最多重试一次，总超时十五秒。如果低相关、连续失败
> 或缺少必要参数，就回退到原有 RAG 或转人工，避免无限循环和成本失控。
>
> 为了能够验证效果，我没有只展示成功截图，而是把评测集从 50 条扩充到 100 条，新增了工具
> 选择准确率、参数正确率、任务成功率、转人工 Precision/Recall、P95 延迟和单任务成本。
> 每次运行还会记录 Trace，可以查看路由、工具、重试、Token 和最终处理结果。
>
> 这个升级不是为了提高功能数量，而是为了证明我能把 LLM 能力做成可评测、可追踪、可降级的
> 业务系统。

## 四、5 分钟技术讲解结构

按照下面顺序讲，不要一开始就陷入代码细节。

### 1. 业务问题

- 旧系统能回答知识问题，但不能处理订单查询。
- 工具失败后缺少统一降级。
- 检索指标有了，但任务成功和转人工质量不清楚。
- 有问题时很难还原完整执行过程。

### 2. 范围控制

- 保留原 RAG 模式和 API。
- 不重构为多 Agent。
- 不接入真实退款、邮件和 CRM。
- 使用本地 mock adapter 验证接口设计。
- 用一个可选模式验证工具路由和评测体系。

### 3. 架构

请求进入后经过：

`输入校验 -> 路由 -> 工具选择 -> 工具执行 -> 结果检查 -> RAG 回退或转人工 -> 输出 -> Trace`

关键状态：

- `tenant_id`
- `session_id`
- `intent`
- `selected_tool`
- `tool_input`
- `tool_output`
- `evidence`
- `retry_count`
- `status`
- `final_answer`
- `handoff_reason`

### 4. 三个核心决策

- 知识检索作为工具复用，而不是复制一套 RAG。
- Agent 工作流作为可选模式，降低回归风险。
- 限制步骤、重试和超时，防止失控。

### 5. 评测

- 原有 RAG 指标继续保留。
- 新增 Agent 任务指标。
- 使用规则评分和 LLM-as-Judge 结合。
- 所有评测数据可重复运行。

### 6. 结果与不足

- 实际达到的指标填写 `[X]`。
- 只验证了本地 mock 和有限任务集。
- 尚未接入真实 CRM 和退款系统。
- 下一阶段是灰度真实流量和扩充评测。

## 五、高频面试问题

### 1. 为什么需要工具调用，不能全部用 RAG？

RAG 适合非结构化知识检索，但订单、库存、工单状态属于实时业务数据。直接让 RAG 检索
mock 文档会产生过期和不一致，因此这类问题应该通过结构化工具读取，再由模型解释结果。

### 2. 如何判断用户该走 RAG 还是工具？

先使用规则和低成本分类模型识别意图，再检查工具所需槽位是否完整。FAQ、政策和售后规则
走 RAG；明确的对象查询走工具；高风险或缺少必要信息时转人工。

### 3. 为什么没有重写为 LangGraph？

当前工作流只有有限状态和少量工具，使用简单 Python 状态机更容易验证、测试和回滚。
引入 LangGraph 会增加迁移和调试成本，但不一定带来业务收益。后续如果出现复杂分支、
checkpoint、人工审批和多轮恢复，再评估迁移。

### 4. 如何避免 Agent 死循环？

- 最多执行三次工具。
- 同一工具最多重试一次。
- 设置十五秒总超时。
- 每次重试必须携带新的错误信息或修正参数。
- 连续失败后直接降级或转人工。

### 5. 如何保证工具参数正确？

工具输入使用 Pydantic 或 JSON Schema 校验，必填字段缺失时先向用户追问；格式错误时不执行
工具，转为结构化错误并决定是否重试。

### 6. 工具失败如何处理？

区分可重试错误和不可重试错误。网络超时、临时服务异常可以重试一次；参数错误、权限错误
和不存在的数据不重试。最终失败后返回明确原因并转人工。

### 7. 如何评测工具选择？

Golden Set 为每个任务标注期望工具和允许的备选工具，统计 Tool Selection Accuracy。
参数评测检查关键字段是否正确，任务评测检查最终答案和动作是否满足业务目标。

### 8. 如何评测转人工？

把转人工视为分类任务，计算 Precision 和 Recall：

- Precision 低：不该转人工的请求被频繁转走。
- Recall 低：高风险或无法处理的请求没有及时转走。

不同业务需要调整阈值，客服政策类问题通常优先保证 Recall。

### 9. 为什么没有全量使用 LLM-as-Judge？

LLM-as-Judge 有成本和波动，容易偏向长答案或同一模型风格。规则明确的指标使用规则评分，
主观指标使用 rubric 和人工抽样校准，避免把评测模型当作绝对真相。

### 10. 为什么原来的 50 条评测集不够？

原评测主要验证检索命中，不覆盖工具选择、多步执行、转人工、延迟和成本。把它扩充到
100 条后，可以覆盖完整任务链路，而不只是检索模块。

### 11. Trace 中记录什么？

记录路由、工具名称、参数摘要、结果状态、耗时、重试、引用、模型、Token、成本和最终状态。
敏感字段在写日志前脱敏。

### 12. 如何定位一次线上失败？

先通过 `trace_id` 查看路由是否正确，再检查工具参数、工具返回、重试和最终决策。如果是
回答错误，再检查引用片段和模型输入，判断问题来自检索、工具、提示词还是业务规则。

### 13. 如何控制成本？

- 简单问题不进入工具工作流。
- 不做无上限规划和重试。
- 控制上下文和工具结果长度。
- 记录每个任务 Token 和成本。
- 对高频问题使用 FAQ 缓存或规则回答。

### 14. 如何避免 Prompt Injection？

- 外部文档和用户输入不能修改系统权限。
- 工具选择由受限路由器和 allowlist 决定。
- 工具参数必须经过 schema 和权限校验。
- 高风险副作用必须审批。
- 建立注入攻击回归测试。

### 15. 多租户如何隔离？

租户信息必须贯穿知识库、会话、Trace、工单和工具调用。检索前加入 tenant metadata filter，
禁止模型通过参数任意切换租户。

### 16. 为什么选择 BGE + FAISS + BM25？

BGE 提供语义召回，BM25 保留编号、型号和专有名词的精确匹配能力；FAISS 适合单机中小规模
检索。当前场景不需要增加独立向量数据库服务。

### 17. 如何解决知识冲突和过期？

文档带版本、生效时间和失效时间，检索时先过滤无效资料；冲突时优先最新有效版本，并在答案
中展示来源。无法确定时拒答或转人工。

### 18. 如何证明不是只做了成功演示？

提供 100 条评测、失败案例、工具错误测试、重试 Trace 和已知限制。演示中主动展示一次
工具失败后的降级，而不是只展示成功路径。

### 19. 如果有真实并发，系统会怎样？

当前先验证单实例和有限并发。继续扩展需要用异步工具调用、连接池、限流、队列和缓存
隔离外部服务；这些是下一阶段计划，不声称当前已经完成。

### 20. 如果重做一次，你会改什么？

更早定义评测集和业务指标，再实现工具。先写失败路径和 Trace，再写成功路径，能减少
后期返工。

## 六、三个失败案例

### 案例一：工具选择错误

问题：

用户问“这个订单能不能退”，路由误判为普通退款政策问题，只返回政策，没有查询订单状态。

定位：

Trace 显示只调用了 `knowledge_search`，没有调用 `order_lookup`。

修复：

增加对象槽位检测，出现订单号时必须先查询订单，再由模型结合退款政策判断。

面试表达：

这说明不能只看最终答案，还要检查工具链路。

### 案例二：低相关 RAG 回答

问题：

知识库没有对应资料时，模型仍然生成了模糊答案。

定位：

检索分数低于阈值，但原流程没有阻断生成。

修复：

增加相关性判断，低于阈值时不生成事实性答案，返回资料不足并转人工。

面试表达：

客服场景中，明确拒答通常比编造答案更可靠。

### 案例三：工具连续失败导致延迟

问题：

订单服务超时后，工作流重复调用，导致响应时间和成本上升。

定位：

Trace 显示同一工具重试次数过多，且每次携带相同参数。

修复：

同一工具最多重试一次，重试前必须修改参数或补充错误上下文，最终失败直接转人工。

面试表达：

Agent 的可靠性不仅是回答正确，还包括失败时能否快速停止并降级。

## 七、项目改动后必须学习的知识

### P0：面试前必须掌握

1. Function Calling 与工具 schema
   - 结构化参数
   - Pydantic 校验
   - 工具权限
   - 错误码

2. 最小 Agent 工作流
   - 路由
   - 状态
   - 重试
   - 超时
   - 降级
   - 人工接管

3. RAG 评测
   - Golden Set
   - Hit@K
   - MRR
   - Citation Accuracy
   - Hallucination Rate

4. Agent 任务评测
   - Task Success
   - Tool Selection
   - Tool Argument Accuracy
   - Handoff Precision/Recall
   - 延迟和成本

5. Trace 与结构化日志
   - trace_id
   - span
   - 工具输入输出
   - Token 和成本
   - 脱敏

6. 失败恢复
   - 可重试错误
   - 不可重试错误
   - 幂等
   - 熔断

### P1：面试中需要能讨论

1. Prompt Injection 和工具越权。
2. 多租户隔离。
3. 限流、超时和成本预算。
4. 异步执行和并发。
5. 文档版本、过期和冲突。
6. Docker、CI 和回归测试。

### P2：拿到面试后再深入

1. LangGraph 的 StateGraph、checkpoint 和 interrupt。
2. MCP 的工具协议和权限边界。
3. GraphRAG、Parent-Child Retrieval。
4. OpenTelemetry、Langfuse 或 Phoenix。
5. Kubernetes 和更大规模部署。

## 八、学习资源

官方文档优先，不要先看大量二手教程：

- OpenAI Function Calling:
  https://platform.openai.com/docs/guides/function-calling
- OpenAI Agents SDK:
  https://openai.github.io/openai-agents-python/
- LangGraph:
  https://langchain-ai.github.io/langgraph/
- Model Context Protocol:
  https://modelcontextprotocol.io/
- Pydantic:
  https://docs.pydantic.dev/latest/
- OpenTelemetry:
  https://opentelemetry.io/docs/
- Langfuse:
  https://langfuse.com/docs

## 九、四周学习计划

### 第 1 周：工具与状态

- 学 Function Calling、Pydantic 和错误处理。
- 能画出工具调用状态图。
- 能解释三个工具为什么使用不同的失败策略。
- 自己手写一个最小状态机。

产出：

- 一张状态图。
- 一份工具 schema。
- 十道工具失败场景问答。

### 第 2 周：评测

- 学检索指标和 Agent 任务指标。
- 理解每条 Golden Set 的标注字段。
- 能解释规则评分与 LLM-as-Judge 的边界。

产出：

- 评测指标说明。
- 一页评测报告。
- 五道评测追问回答。

### 第 3 周：Trace 与安全

- 学 trace、span、结构化日志和脱敏。
- 学 Prompt Injection、权限和幂等。

产出：

- 一次失败请求的完整 Trace 复盘。
- 十个安全测试案例。

### 第 4 周：项目表达与面试模拟

- 练习 30 秒、2 分钟和 5 分钟讲解。
- 练习系统设计题。
- 模拟三次面试追问。

产出：

- 30 分钟项目深挖稿。
- 三个失败案例。
- 一页项目案例。

## 十、简历项目描述模板

所有 `[X]` 必须替换为真实数据。

> 在现有企业客服 RAG 上新增可选工具调用工作流，支持知识检索、订单查询和人工转接；
> 设计最大步骤、超时、重试和降级机制，避免 Agent 循环和成本失控。

> 构建 100 条任务的回归评测集，覆盖 FAQ、订单、物流、退款、转人工、多轮和注入攻击；
> 工具选择准确率 `[X]%`，任务成功率 `[X]%`，引用准确率 `[X]%`。

> 增加完整执行 Trace，记录路由、工具调用、重试、Token、成本和最终状态；
> P95 延迟为 `[X]` 秒，单任务成本为 `[X]`，原有测试和新测试全部通过。

## 十一、作品集交付清单

1. GitHub README。
2. 架构图和数据流图。
3. 三个工具 Schema。
4. 100 条 Golden Set。
5. 一条命令运行评测。
6. 评测 Markdown 和 JSON 报告。
7. Trace 页面截图。
8. 工具失败的 Trace 截图。
9. 3-5 分钟演示视频。
10. 一页项目案例。
11. 三个失败案例。
12. 已知限制和下一阶段计划。

## 十二、面试前最终检查

1. 我能在 30 秒内说明项目升级了什么。
2. 我能在 2 分钟内讲清完整链路。
3. 我能在 5 分钟内讲清三个关键设计决策。
4. 我能解释为什么不重写为复杂多 Agent。
5. 我能解释如何避免循环、超时和成本失控。
6. 我能解释每个评测指标如何计算。
7. 我能打开 Trace 还原一次失败。
8. 我能主动说出项目的限制。
9. 我不会声称 mock 系统已经承载真实线上流量。
10. 我的简历指标都能从项目中复现。
