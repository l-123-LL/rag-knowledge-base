services:
  backend:
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    ports:
      - "8000:8000"

  web:
    build:
      context: ./web
    depends_on:
      - backend
    ports:
      - "5173:80"
