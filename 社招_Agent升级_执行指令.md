# 社招 Agent 升级 · 执行指令（Phase 2 起适用）

> 本指令是《社招_Agent升级总指令.md》的执行补充，两者冲突时以本文件为准。
> 目的：一份指令解决四件事——少跑测试、复用开源、压缩工时、授权一次到位，并强制"最小改动"。

## 0. 目标与路线

在保留现有可用功能和测试的前提下，把项目升级为 ServiceOps Agent。路线固定为**增量 + 分层重建**：

- 保留并复用：检索链路、知识库导入、FAQ、意图路由、转人工与工单、会话、多租户、可观测、成本、备份。
- 重写并新增：Agent 控制层（状态机 / 节点 / 工具执行 / checkpoint / 审批）。
- 前端：只新增页面，不重构现有组件。

不做整体重写，不引入整站平台替换现有实现。

## 1. 权限授权（一次到位）

以下权限视为**已授权**，执行期间不要因为等待确认而停下：

- 文件：在 `D:\rag知识库` 内创建、修改、移动、删除文件与目录，包括临时文件和运行时数据。
- Git：`git add`、`git commit`（本项目，允许一次改动一个 commit）。
- 网络：`pip install` / `npm install` / `npm ci`、调用 DeepSeek API（评测与验证用）、下载 HuggingFace 模型、拉取 Docker 镜像、访问 GitHub API 查询开源项目。
- 进程：启动与结束本项目相关进程（端口 8000 / 5173 / 5174 / 4173），包括清理残留进程。
- 工具：启动无头浏览器做截图与版面校验、打开 VS Code 与本地页面。

仍然禁止：

- `git push`、`git reset --hard`、`git clean -fdx` 等不可逆或对外部生效的操作。
- 删除用户个人文件、修改项目目录之外的任何内容。
- 把 API Key、密码、token 写入代码、文档、日志、追踪记录或 Git。

## 2. 执行方式（减少往返）

- 按批次连续推进，**只在需要架构拍板时停一次**；其余决策按本指令默认值直接执行。
- 每个批次结束必须：跑测试 → 评测（按分档） → 更新 `PROGRESS.md` → `git commit`。
- 每批汇报固定六项：本批目标 / 已完成 / 改动文件与接口 / 测试与评测结果 / 新旧指标对比 / 风险与下一批建议。
- 不为"确认小事"中断，例如文件命名、目录位置、日志字段名、mock 数据内容。

## 3. 最小改动原则（硬约束）

1. 只扩展、不替换：`retrieval.py`、`embeddings.py`、`vector_store.py`、`chunking.py`、`ingestion.py`、`faq_store.py`、`intent.py`、`ticket_store.py`、`session_store.py`、`tenant.py`、`observability.py`、`cost.py`、`backup.py` 的现有公开函数签名保持不变，新增能力以新函数或新参数叠加。
2. 控制流改造限定在 `pipeline.py` 与 `main.py`：新增 Agent 编排层，旧 `/ask` 直连路径保留为 fallback，用环境变量开关切换（默认开启新路径，出问题可一键回退）。
3. 前端不动现有组件结构，新页面沿用现有 Tailwind 令牌与视觉规范。
4. 不删除任何现有测试；每新增一项能力必须同时新增测试与失败路径测试。
5. 每个批次结束时，项目必须仍然可运行、可演示、全部测试通过。

## 4. 测试策略（减少测试但保住完成度）

核心原则：**减频次，不减范围**。里程碑必须跑满并留档，简历指标才可复现。

| 层级 | 触发时机 | 内容 | 预算 |
| --- | --- | --- | --- |
| 提交级 | 每次改动 | 相关模块单测（`pytest -k`、vitest 单文件）+ 20–30 条冒烟评测 | 约 30 秒，约 6 万 token |
| 批次级 | 每个工作流收尾 | 全量单测 + 100 条核心集 | 约 2 分钟，约 30 万 token |
| 里程碑级 | 每个里程碑 | 全量单测 + 150–200 条全集 + 全量裁判 + 抽 10% 人工复核 | 约 10 分钟，约 120 万 token |

必须落地的四件事：

1. 前端测试分环境：纯逻辑测试用 node 环境，只有组件测试用 jsdom（当前 30 秒里 27 秒是 jsdom 初始化，用例本身 0.5 秒）。
2. 评测脚本支持 `--limit` / `--tag smoke|core|full` 分档参数。
3. LLM-as-Judge 只跑失败样本 + 10% 随机抽样，不做全量。
4. 检索结果按 `dataset_version + git_sha` 缓存；FAQ 命中的用例不调用模型。

不可削减的三块（属于验收标准，砍了要扣分）：工具失败路径测试（超时 / 越权 / 幂等重复）、安全测试集（注入、伪造租户、诱导越权、超长上下文、重复副作用）、里程碑全量回归与历史留档。

## 5. 开源复用清单

| 用途 | 采用 | 理由 | 许可 |
| --- | --- | --- | --- |
| 追踪与可观测 UI | Langfuse；不可用则 Phoenix | 省掉自建 Trace 页 | 需确认（MIT + 企业版混合） |
| 评测指标 | DeepEval | 维护活跃（2026-09-16 仍在提交），指标齐全 | Apache-2.0 |
| PII 检测与脱敏 | Presidio | 现成的手机号 / 邮箱 / 证件脱敏 | MIT |
| Mock 业务后端 | json-server | 快速造订单 / 物流假接口 | MIT |
| 设计参考（不引依赖） | LangGraph、Dify、RAGFlow、FastGPT | 只读设计，不复制代码 | 部分许可不明 |

规则：

- Agent 状态机自写，不引 LangGraph 运行时；LangGraph 只作设计参考，面试时作为"评估后选择自写"的对比项。
- 不使用 RAGAS（最近提交停在 2026-02-24，维护停滞）。
- 不直接部署或改造 Dify / RAGFlow / FastGPT 整站，它们会覆盖项目定位。
- 不复制许可不明的代码，界面结构可以借鉴。
- 每引入一个依赖必须：登记到 `requirements.txt` 或 `package.json`、补 `.env.example`、写明降级路径（依赖不可用时系统如何退化）、在文档记录许可。

## 6. 时间压缩手段（已确认）

| 手段 | 节省 |
| --- | --- |
| 权限一次到位 | 1.5–3 小时 |
| 测试按需 + 分环境 | 4–6 小时 |
| 评测分档与抽样 | 70–80% 评测 token |
| 复用现有评测集、文档、架构图、启动脚本 | 3–5 小时 |
| 开源复用（4 个精选库） | 6–9 小时 |

目标：全量 26–46 小时（含开源复用）；最小可面试版 14–20 小时。
不为省时间砍掉：Agent 状态机、工具权限与幂等、评测体系、里程碑留档。

## 7. 范围与里程碑

必做：Agent 状态机、4 个核心工具（`knowledge_search` / `order_lookup` / `refund_policy_check` / `create_ticket`）、120 条评测集、Trace 页、审批页、一条命令启动、一页案例。

可延后：`parent-child` 检索、知识版本热更新、CRM 真实对接、评测趋势页、成本页、技术文章（1 篇即可）。

| 里程碑 | 内容 | 验收标准 |
| --- | --- | --- |
| M1 基线收尾 | 修评测口径、BM25 O(N²)、前端 Key 泄露、鉴权默认值、价格配置与成本换算 | 评测走真实链路、Key 不出现在构建产物、成本字段有值、全量测试绿 |
| M2 Agent 骨架 | 状态机 + 节点 + checkpoint + 预算/步数/超时 + Trace | 每节点单测；步数上限生效；Run 可恢复；有 trace_id |
| M3 工具与审批 | 4 个核心工具 + schema/权限/幂等/dry-run/审批 | 每个工具有成功/失败/超时/越权测试；重复提交只落一次 |
| M4 评测与交付 | 120 条评测集 + 分档回归 + Trace 页 + 审批页 + 演示脚本 | 一条命令产出全指标报告；简历数字可复现 |

## 8. 红线

- 不伪造指标、用户量、并发量；模拟数据必须标注为模拟并说明替换路径。
- 外部写操作默认 dry-run；退款、发信、更新 CRM 等必须人工审批后执行。
- 不把 Key、完整提示词、用户隐私写入日志与追踪。
- 每次改动必须 commit，交付前必须测试通过。
