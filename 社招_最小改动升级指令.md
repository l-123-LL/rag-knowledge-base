# 社招导向的最小改动升级指令

在 `D:\rag知识库` 中修改当前企业智能客服 RAG 项目。目标是用尽可能小的改动，
让项目达到社招面试可讲、可演示、可验证的标准，不重写架构，不追求完整 Agent 平台。

## 一、硬性约束

1. 保留现有项目名称、REST API、前后端结构和主要业务流程。
2. 不整体改名为 ServiceOps Agent，不把项目包装成全新产品。
3. 不强制引入 LangGraph，不实现多 Agent，不重写现有 RAG Pipeline。
4. Agent 工作流必须作为可选模式接入，默认关闭，现有模式继续可用。
5. 优先新增独立模块，少量修改现有入口和前端页面。
6. 目标代码规模：新增不超过 8-12 个文件，修改不超过 8 个既有文件，
   新增和改动代码尽量控制在 800-1200 行，不含测试与评测数据。
7. 不接真实 CRM、邮件、退款、支付和物流系统；使用本地 mock adapter。
8. 所有外部写操作只做 dry-run 或本地工单，不产生真实副作用。
9. 不允许伪造指标、并发量、上线数据或业务收益。
10. 每阶段完成后运行现有测试，并保证原有功能不回归。

## 二、只做六个 P0 改动

### P0-1 增加可选工具调用模式

在现有请求入口增加可选参数：

`workflow_mode = "rag" | "tools"`

- `rag`：保持当前行为，默认值。
- `tools`：进入最小工具工作流。

至少提供三个工具：

1. `knowledge_search`：直接复用现有混合检索和引用能力。
2. `order_lookup`：读取本地 mock 订单数据。
3. `human_handoff`：创建本地工单并返回转人工结果。

可选增加 `logistics_track`，但不是必须。

每个工具必须包含：

- Pydantic 或 JSON Schema 输入输出。
- 参数校验和统一错误码。
- 超时、重试和结构化日志。
- 单元测试。

不要求实现退款、发邮件、更新 CRM 等副作用工具。

### P0-2 增加最小工作流状态

仅定义以下必要状态：

```text
tenant_id
session_id
user_input
intent
selected_tool
tool_input
tool_output
evidence
retry_count
status
final_answer
handoff_reason
```

工作流规则：

- 最多 3 次工具调用。
- 同一工具最多重试 1 次。
- 总超时 15 秒。
- 无合适工具时回到原 RAG。
- RAG 低相关或工具连续失败时转人工。
- 不允许循环规划和无限重试。

工作流可以写成简单 Python 状态机，不需要 LangGraph。

### P0-3 增加最小执行轨迹

每次任务生成 `trace_id`，记录：

- 路由结果。
- 每次工具名称、参数摘要、耗时和状态。
- 重试次数和失败原因。
- RAG 引用数量。
- 模型输入输出 Token。
- 总延迟和估算成本。
- 最终是回答、拒答还是转人工。

最小实现：

- 使用现有 JSONL 日志机制写入 trace。
- 增加 `GET /traces/{trace_id}`。
- 前端增加一个简单时间线，不开发独立监控平台。

日志必须脱敏，不记录手机号、邮箱、地址、证件号和 API Key。

### P0-4 把评测集从 50 条扩充到 100 条

保留现有 50 条，再增加 50 条任务：

| 类型 | 数量 |
| --- | ---: |
| FAQ | 20 |
| 订单查询 | 15 |
| 物流查询 | 15 |
| 退款政策 | 15 |
| 投诉与转人工 | 15 |
| 多轮与工具组合 | 10 |
| 知识不足、拒答、Prompt Injection | 10 |

评测至少输出：

- Task Success Rate
- Tool Selection Accuracy
- Tool Argument Accuracy
- Citation Accuracy
- Hallucination / Invalid Answer Rate
- Handoff Precision 和 Recall
- P50 / P95 延迟
- 平均 Token 和成本
- 平均工具调用次数和重试率

要求：

- 提供一条命令运行全部评测。
- 输出 JSON 和 Markdown 报告。
- 评测必须可重复。
- LLM-as-Judge 只用于辅助评分，关键指标保留规则评分或人工抽样。

### P0-5 增加一个完整演示场景

只做一个端到端演示，覆盖：

1. 普通 FAQ 直接引用 RAG 回答。
2. 订单问题调用 `order_lookup` 后再回答。
3. 连续失败或低相关时调用 `human_handoff`。
4. Trace 页面展示路由、工具、重试、Token、成本和处理结果。

不要为每个工具分别制作演示，不开发复杂新页面。

### P0-6 补充面试材料

只更新以下内容：

1. README 增加新的可选工作流、架构图、评测命令和结果表。
2. 增加一页项目案例：问题、方案、关键决策、指标、失败和迭代。
3. 保存三张关键截图：工具调用、Trace、评测结果。
4. 准备三个失败案例：
   - 工具选择错误。
   - 工具连续失败后的降级。
   - 低相关检索触发的拒答和转人工。
5. 准备五个面试问题：
   - 为什么采用可选工具工作流？
   - 如何限制步骤、成本和死循环？
   - 如何评测工具选择和参数是否正确？
   - 如何处理低相关检索和转人工？
   - 如何通过 Trace 定位一次失败？

## 三、明确不做

以下内容全部排除，不要加入实施范围：

- LangGraph 或 LangChain 重写。
- 多 Agent、Supervisor、长程自主规划。
- Redis、PostgreSQL、消息队列的新基础设施。
- Kubernetes、服务网格和复杂云部署。
- 真实 CRM、邮件、支付、退款和物流接入。
- 150-200 条评测集。
- 完整可观测性平台。
- 复杂长期记忆和知识图谱。
- 新前端控制台。
- 项目改名和品牌化。

## 四、最低验收标准

完成后必须满足：

1. 原有测试全部通过。
2. 新增至少 20 项测试，覆盖工具、状态流转、重试、脱敏和转人工。
3. `workflow_mode="rag"` 行为与升级前保持一致。
4. `workflow_mode="tools"` 能完成 FAQ、订单查询和转人工三条路径。
5. 100 条评测可一条命令运行并生成报告。
6. 每次任务可以通过 `trace_id` 还原执行过程。
7. 工具调用有超时、重试、最大次数和失败降级。
8. 简历中的每项新指标都能从评测或 Trace 中复现。

## 五、执行顺序

### 阶段 0：现状审计

只读文档和核心入口，不修改代码。输出：

- 可复用的现有模块。
- 需要新增的文件。
- 需要修改的现有文件。
- 预计代码改动量。
- 2-3 周实施计划。
- 风险清单。

等待确认后继续。

### 阶段 1：工具与状态

实现 `workflow_mode`、三个工具和最小状态机。

### 阶段 2：Trace 与前端

实现 trace 写入、查询接口和简单时间线。

### 阶段 3：评测

扩充到 100 条任务，生成统一评测报告。

### 阶段 4：文档与面试材料

更新 README、项目案例、截图和失败复盘。

## 六、第一次执行指令

第一次只执行阶段 0，不修改业务代码。输出最小改动方案、预计文件清单、2-3 周计划
和验收风险。停止并等待确认。
