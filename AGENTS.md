# 注意事项

第一点，每次改动后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。

第二点，每次改动过后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。

---

# 项目文档（新会话开工前必读）

- `PROGRESS.md`：当前进度、实测数字、下一步计划。**每次开工先读这份**，它记录做到哪了。
- `HANDOFF.md`：项目目标、文件地图、技术决策、约束和运行方式。
- 两份文档要保持同步：进度变化更新 `PROGRESS.md`，架构或决策变化更新 `HANDOFF.md`。
