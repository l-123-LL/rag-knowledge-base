# 社招 Agent 最小改动升级 · 执行指令

> 范围锚点：本文件与《社招_最小改动升级指令.md》配套使用，**范围以那一份为准**，本文件补充"执行方式、测试策略、开源策略、权限授权、时间预算"。
> 与《社招_Agent升级总指令.md》冲突时，一律以最小改动版为准，不扩张范围、不改项目定位。
> 本文件取代此前草稿《社招_Agent升级_执行指令.md》。

---

## 一、范围锚定（禁止扩张）

只做《社招_最小改动升级指令.md》里的六个 P0，不做额外功能：

1. `workflow_mode = "rag" | "tools"` 可选工具工作流（默认 `rag`）。
2. 最小工作流状态（最多 3 次工具调用、同工具重试 1 次、总超时 15 秒）。
3. 最小执行轨迹 `trace_id` + `GET /traces/{trace_id}` + 前端时间线。
4. 评测集 50 → 100 条，一条命令出 JSON + Markdown 报告。
5. 一个端到端演示场景（FAQ / 订单工具 / 转人工 / Trace）。
6. 面试材料（README 更新、一页案例、三张截图、三个失败案例、五个面试问答）。

明确排除：LangGraph 或 LangChain 重写、多 Agent、长程规划、Redis / PostgreSQL / 消息队列、K8s、真实 CRM / 邮件 / 支付 / 退款 / 物流接入、150–200 条评测、完整可观测平台、复杂长期记忆、新前端控制台、项目改名。

**代码预算**：新增不超过 12 个文件，修改不超过 8 个既有文件，净增 800–1200 行（不含测试与评测数据）。每次提交前自检，超标必须在汇报里说明理由。

---

## 二、权限一次到位

以下视为**已授权**，执行期间不要因为等待确认而中断：

- 文件：在 `D:\rag知识库` 内创建、修改、移动文件与目录（含临时文件、运行时数据）。
- Git：`git add`、`git commit`（本项目，允许一个改动一个 commit）。
- 网络：`pip install` / `npm install` / `npm ci`、调用 DeepSeek API（评测与验证）、下载 HuggingFace 模型、查询 GitHub API、拉取 Docker 镜像。
- 进程：启动与结束本项目相关进程（8000 / 5173 / 5174 / 4173），含清理残留进程。
- 工具：无头浏览器截图与版面校验、打开 VS Code 与本地页面。

仍然禁止：`git push`、`git reset --hard`、`git clean -fdx`；删除用户个人文件；改动项目目录之外的内容；把 Key、密码、token 写入代码、文档、日志或追踪记录。

---

## 三、执行方式（减少往返）

- 严格按《社招_最小改动升级指令.md》的阶段 0 → 4 顺序推进。
- 每阶段结束：跑测试 → 跑评测（按分档） → 更新 `PROGRESS.md` → `git commit` → 汇报六项（目标 / 已完成 / 改动文件与接口 / 测试与评测结果 / 新旧指标对比 / 风险与下一批建议）。
- **只在阶段边界和架构拍板时停**，其余一律按下面"默认值"直接执行，不再逐项确认。

**默认值清单（避免来回问）**

| 决策点 | 默认选择 |
| --- | --- |
| mock 数据形态 | 本地 JSON 文件 + 加载函数，不起额外服务 |
| PII 脱敏 | 自写正则脱敏（手机号 / 邮箱 / 身份证 / 地址 / Key），不引 Presidio |
| 评测评分 | 规则评分优先，LLM-as-Judge 只做辅助与抽样 |
| Trace 存储 | 复用现有 JSONL 机制，加一个查询接口与前端时间线页 |
| 工具超时与重试 | 单工具超时 3 秒，同工具重试 1 次，工作流总超时 15 秒 |
| 订单 mock 规模 | 20 条订单、10 条物流记录，覆盖正常 / 找不到 / 参数缺失三类 |
| 前端新页面 | 只加一个 Trace 时间线页，沿用现有 Tailwind 令牌 |

---

## 四、最小改动硬约束

1. **签名不变，只加不改**：`retrieval.py`、`embeddings.py`、`vector_store.py`、`chunking.py`、`ingestion.py`、`faq_store.py`、`intent.py`、`ticket_store.py`、`session_store.py`、`tenant.py`、`observability.py`、`cost.py`、`backup.py` 的现有公开函数签名保持原样，新增能力以新模块或新参数叠加。
2. **控制流改造限定在 `main.py` 与 `pipeline.py`**：新增独立的工具工作流模块，`workflow_mode="rag"` 必须与升级前行为完全一致。
3. **不删除任何现有测试**；每个新能力必须同时写成功路径与失败路径测试。
4. **每阶段结束项目必须仍可运行、可演示、测试全绿**。
5. 新增文件命名与现有风格一致，不引入新的目录层级（除非确有必要并在汇报里说明）。

---

## 五、测试策略（减少测试，保住完成度）

核心原则：**减频次，不减范围**。里程碑必须跑满并留档，简历指标才可复现。

| 层级 | 触发时机 | 内容 | 预算 |
| --- | --- | --- | --- |
| 提交级 | 每次改动 | 相关模块单测（`pytest -k`、vitest 单文件）+ 20 条冒烟评测 | 约 30 秒，约 6 万 token |
| 阶段级 | 每个阶段收尾 | 全量单测 + 50 条核心评测 | 约 2 分钟，约 15 万 token |
| 里程碑级 | 阶段 3 与最终交付 | 全量单测 + 100 条全集 + 抽样人工复核 | 约 8 分钟，约 50 万 token |

必须落地的四件事：

1. **前端测试分环境**：纯逻辑测试用 node 环境，只有组件测试用 jsdom。当前一轮 32.9 秒里 `tests` 只占 0.5 秒、`setup` 占 30.3 秒，分环境后预计降到 5–10 秒。
2. 评测脚本支持 `--limit` 与 `--tag smoke|core|full` 分档。
3. LLM-as-Judge 只跑失败样本 + 10% 随机抽样。
4. 检索结果按 `dataset_version + git_sha` 缓存；FAQ 命中用例不调用模型。

不可削减：工具失败路径测试（超时 / 越权 / 幂等重复）、脱敏测试、转人工测试、100 条评测的完整留档。

---

## 六、开源策略（最小版）

**默认不引新依赖。** 这个范围内能省的时间主要来自复用你已有的代码，而不是开源库。

| 用途 | 结论 | 说明 |
| --- | --- | --- |
| 状态机 | 自写 | 最小改动版明确不引 LangGraph；只读它的状态图写法作参考 |
| 追踪 / 评测平台 | 不引入 | 明确排除"完整可观测性平台"，用现有 JSONL + 一个时间线页 |
| mock 业务数据 | 自写 JSON | 比引 json-server 更轻，无需额外进程 |
| PII 脱敏 | 自写正则 | 当前需求覆盖手机号 / 邮箱 / 身份证 / 地址 / Key，30 行即可；仅当要求覆盖更多类型再考虑 Presidio（MIT） |
| 生成质量指标 | 复用现有 `judge.py` | 不引 DeepEval / RAGAS，避免为标准化指标引入重依赖 |
| 设计参考（只读，不复制代码） | LangGraph 的状态机、Dify / RAGFlow 的工具与追踪界面 | 许可不明的代码不得复制；借鉴结构要在文档注明来源与理由 |

规则：每引入一个依赖前必须先说明"省了多少时间、带来什么风险、不可用时如何降级"，并在汇报中记录；不得为了"看起来用了开源"而引入依赖。

---

## 七、时间与预算

| 阶段 | 内容 | 预算 |
| --- | --- | --- |
| 阶段 0 | 最小改动方案（复用清单 / 新增文件 / 待改文件 / 改动量 / 风险） | 0.5–1 h |
| 阶段 1 | `workflow_mode` + 3 个工具 + 最小状态机 | 4–6 h |
| 阶段 2 | Trace 写入、查询接口、前端时间线、脱敏 | 2–3 h |
| 阶段 3 | 评测扩到 100 条 + 统一报告 | 3–5 h |
| 阶段 4 | README、一页案例、截图、失败复盘 | 1.5–2 h |
| **合计** |  | **12–18 h** |

token 与花费（DeepSeek 官方价位，Flash 空闲价）：开发消耗约 10–16M token（约 ¥12–20）；评测 API 约 200–400 万 token（约 ¥5–15）；**总计约 ¥20–40**。若用 Pro 档模型，约为其 4 倍。

---

## 八、验收与红线

验收（沿用最小改动版第八条，并补充执行要求）：

1. 原有测试全部通过，`workflow_mode="rag"` 行为与升级前一致。
2. 新增至少 20 项测试，覆盖工具、状态流转、重试、脱敏与转人工。
3. `workflow_mode="tools"` 能走通 FAQ、订单查询、转人工三条路径。
4. 100 条评测一条命令跑完并生成 JSON + Markdown 报告，历史结果留档。
5. 每次任务可用 `trace_id` 还原：路由、工具、重试、token、成本、最终结果。
6. 工具调用有超时、重试、最大次数与失败降级。
7. 简历里的每项新指标都能从评测或 Trace 中复现。
8. 代码改动量在预算内（新增 ≤12 文件、修改 ≤8 文件、净增 800–1200 行）。

红线：不伪造指标与用户量；mock 数据必须标注为模拟并说明替换路径；外部写操作只做 dry-run 或本地工单；不把 Key、完整提示词、用户隐私写入日志与追踪；每次改动必须 commit，交付前必须测试通过。

---

## 九、第一次执行

从阶段 0 开始：只读相关文档与核心入口，输出"可复用模块清单 / 需新增文件 / 需修改文件 / 预计改动行数 / 分阶段计划 / 风险清单"，不修改业务代码，等待确认后进入阶段 1。
