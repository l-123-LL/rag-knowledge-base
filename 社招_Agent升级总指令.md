# 社招导向的 ServiceOps Agent 升级总指令

你现在负责把当前企业智能客服 RAG 项目升级为一个可用于社招面试和生产演示的
ServiceOps Agent 系统。目标不是继续增加普通聊天功能，而是把它做成一个具备真实
业务流程、工具调用、状态管理、评测、可观测性、安全边界和可交付文档的完整项目。

## 一、项目定位

目标岗位：

- AI 应用开发工程师
- LLM 应用工程师
- Agent 开发工程师
- RAG / 检索增强工程师
- AI 后端工程师

面试定位：

> 我有把 LLM 能力工程化为可靠业务系统，并能通过评测、追踪和护栏持续优化线上
> 结果的完整经验。

项目升级后的名称：

**ServiceOps Agent - 企业客服与售后处理智能体**

不要把它包装成“大模型算法科研项目”。这是一个生产级应用工程和 Agent 工程项目。

## 二、执行原则

1. 先审计、再设计、后实现。第一次执行只做现状审计和升级方案，不要直接大规模改代码。
2. 优先提高可靠性、可评测性和可观测性，不追求功能数量。
3. 保留现有可用功能和测试，增量改造，避免一次性重写。
4. 所有新增能力必须有明确接口、测试、失败路径和验收标准。
5. 不允许伪造指标、用户量、并发量或业务效果。
6. 如果使用模拟数据或模拟外部系统，必须明确标注为模拟，并说明如何替换为真实接口。
7. 外部副作用工具默认只允许 dry-run；退款、发邮件、更新 CRM、提交工单等真实写入
   必须经过显式确认或人工审批。
8. 不把敏感数据、完整提示词、API Key 或用户信息写入日志和追踪记录。
9. 每完成一个阶段，必须运行测试、评测和构建，并更新文档与指标。
10. 每个技术决策都要能回答：为什么选、备选方案是什么、权衡是什么、失败时怎么办、
    如何验证线上效果。

## 三、第一步：只做现状审计

先阅读以下文件和相关入口代码，不要逐行扫描整个仓库：

- `D:\rag知识库\HANDOFF.md`
- `D:\rag知识库\INTERVIEW.md`
- `D:\rag知识库\PROGRESS.md`
- 后端入口、RAG 管线、检索、生成、评测、session、tenant、ticket、observability 相关模块
- 前端核心页面与 API 接线
- 现有测试、CI、Docker 配置和依赖文件

第一阶段只输出以下内容，不修改业务代码：

1. 当前系统架构图。
2. 当前已具备能力清单。
3. 距离生产级 ServiceOps Agent 的能力缺口。
4. 高风险技术债和可能影响演示的阻塞项。
5. 目标架构图。
6. 数据模型和状态模型建议。
7. 工具清单和权限边界。
8. 评测指标基线与待补齐指标。
9. 8-12 周分阶段实施计划。
10. 每阶段的测试、验收标准和可交付物。

审计完成后等待确认，再进入第二阶段。

## 四、目标 Agent 架构

把主流程改成清晰、可追踪、可测试的 Agent 工作流：

`请求接入 -> 意图路由 -> 计划 -> 工具调用 -> 观察结果 -> 质量判断 ->
重试/回答/人工接管`

建议节点：

1. `InputGuard`：输入校验、租户识别、敏感信息检测、Prompt Injection 初筛。
2. `Router`：识别 FAQ、订单、物流、退款、投诉、闲聊和需要人工处理的请求。
3. `Planner`：只在确实存在多步任务时生成计划，避免所有问题都强制走复杂 Agent。
4. `ToolExecutor`：按结构化 schema 执行工具，记录输入、输出、耗时、状态和重试。
5. `Judge`：检查答案是否有证据、工具结果是否完整、动作是否满足权限要求。
6. `Responder`：生成简洁回答，附结构化引用、执行步骤和下一步建议。
7. `HumanHandoff`：资料不足、高风险动作、连续失败或用户明确要求时转人工。
8. `MemoryWriter`：写入会话摘要、待办状态、已执行动作和长期偏好。
9. `AuditLogger`：记录完整工作流轨迹、审批记录和异常。

实现要求：

- 优先使用 LangGraph 或显式状态机；如果引入框架，必须解释收益并保留可替换接口。
- 所有状态使用 Pydantic 或等价的结构化模型定义。
- 支持 checkpoint、恢复、最大步骤数、超时、重试上限和幂等执行。
- 不允许无上限循环。
- 每个节点和工具调用必须有独立 span 或结构化日志。

## 五、必须接入的业务工具

至少实现以下工具，并优先使用本地可重复的 mock service：

1. `knowledge_search`
2. `order_lookup`
3. `logistics_track`
4. `refund_policy_check`
5. `create_ticket`
6. `human_handoff`
7. `crm_update`

每个工具必须具备：

- JSON Schema 或 Pydantic 输入输出定义。
- 参数校验和错误码。
- 权限校验。
- 超时、重试和幂等策略。
- 结构化日志。
- 单元测试和失败场景测试。
- 可替换的真实系统 adapter 接口。

## 六、RAG 能力升级

在保留现有 BGE + FAISS + BM25 混合检索的基础上，增加：

1. Query Rewrite 和多轮指代消解。
2. Metadata Filter，支持租户、文档类型、版本、地区和生效时间过滤。
3. Parent-Child Retrieval，提升长文档上下文完整性。
4. Rerank 候选池与降级策略。
5. 文档版本、失效时间和知识库热更新。
6. 引用校验：答案中的关键结论必须有可定位证据。
7. 无答案识别：低相关时不调用模型强行生成答案。
8. 检索结果去重、去冲突和新鲜度排序。

## 七、评测体系

建立版本化的 Golden Set，至少包含 150-200 条任务，覆盖：

- FAQ
- 订单查询
- 物流查询
- 退款政策
- 投诉与转人工
- 多轮上下文和工具组合
- 知识不足与拒答
- Prompt Injection 与越权工具调用

必须输出以下指标：

- Task Success Rate
- Tool Selection Accuracy
- Tool Argument Accuracy
- Citation Accuracy
- Hallucination Rate
- Escalation Precision / Recall
- 自动解决率
- 平均步骤数和重试次数
- P50 / P95 端到端延迟
- 单任务 Token 和成本
- 失败恢复率
- 并发下的错误率和 P95 延迟

评测要求：

1. 评测数据、评分规则和运行脚本必须入库版本管理。
2. RAG 指标和 Agent 任务指标分开统计。
3. LLM-as-Judge 必须定义 rubric，并抽样人工复核。
4. 每次重要提交都要跑回归评测，并保留历史结果。
5. 所有目标值先基于现状基线确定，不能凭空写目标。

社招展示时建议至少达到或解释以下方向：

- Task Success：目标 85% 以上。
- Tool Selection：目标 90% 以上。
- Citation Accuracy：目标 95% 以上。
- Hallucination Rate：目标 2% 以下。
- Human Handoff Recall：目标 95% 以上。
- 工具、引用、转人工和成本指标必须可复现。

如果环境或数据限制导致达不到目标，必须说明原因、边界和下一步方案。

## 八、可观测性

为每次 Agent Run 生成 `trace_id`，记录：

- 请求路由和计划
- 每个节点耗时
- 工具名称、参数摘要、状态和耗时
- 检索命中的文档 ID 和引用
- 模型名称、输入输出 token、成本和延迟
- 重试、失败、降级和人工接管
- 最终答案、反馈和工单状态

实现要求：

- 优先接入 OpenTelemetry，并根据可维护性选择 Langfuse、Phoenix 或本地 tracing。
- 对手机号、邮箱、地址、证件号和 API Key 做脱敏。
- 提供评测结果页、Agent Trace 页和成本统计页。
- 支持按租户、任务类型、模型和时间范围聚合。

## 九、安全、权限与治理

必须实现并测试：

1. Tenant isolation。
2. RBAC 和工具级权限。
3. Prompt Injection 防护。
4. Tool allowlist。
5. 最大步骤数、最大耗时和最大成本。
6. 外部写操作 dry-run 和人工审批。
7. 幂等键，避免重复退款、重复建单或重复发信。
8. 熔断、限流、退避重试和降级。
9. 审计日志和不可篡改记录。
10. 数据保留、删除和脱敏策略。

建立安全测试集，至少覆盖：

- 提示词越权。
- 伪造用户身份或租户。
- 诱导调用未授权工具。
- 重复提交副作用动作。
- 超长上下文和成本攻击。

## 十、前端与演示体验

前端不能只是聊天框。至少包含：

- 聊天与结构化引用。
- Agent 执行步骤和工具调用可视化。
- 人工审批界面。
- 工单状态和转人工记录。
- 知识库来源与版本。
- Trace 查询和错误诊断。
- 评测结果与指标趋势。

准备一个 3-5 分钟演示脚本，必须包含：

1. 正常 FAQ。
2. 多步订单和物流查询。
3. 退款政策工具调用与人工审批。
4. 知识不足时拒答并转人工。
5. 工具失败后的重试和降级。
6. Trace 页展示完整链路、token、成本和最终结果。

## 十一、部署与生产工程

交付时必须具备：

- Docker Compose 一键启动。
- `.env.example` 和完整配置说明。
- 数据库迁移和初始化数据。
- 健康检查、就绪检查和服务依赖检查。
- GitHub Actions 自动测试、类型检查和构建。
- 单元测试、集成测试、端到端测试。
- 并发和负载测试脚本。
- 故障注入与恢复测试。
- 备份、恢复和回滚说明。

## 十二、公开作品集与社招证据

项目最终必须能作为社招作品集展示：

1. README 包含项目背景、架构图、核心流程、演示入口和评测结果。
2. 提供可在线体验的 Demo，或者可稳定复现的本地一条命令启动方式。
3. 提供架构图、数据流图、状态图和工具权限图。
4. 提供评测报告和失败案例。
5. 提供 3-5 分钟演示视频。
6. 提供一页项目案例：问题、方案、关键决策、指标、失败与迭代。
7. 提供 2-3 篇技术文章：
   - 从 RAG 到 Agent 工作流的升级
   - 工具调用 Agent 的评测与失败恢复
   - Agent 权限、幂等和人工审批护栏

简历表述必须以真实结果为准，例如：

- 把平均处理时间从 X 降到 Y。
- 自动解决率达到 X%。
- 转人工准确率达到 X%。
- 单任务成本下降 X%。
- P95 延迟控制在 X 秒。
- 回归评测覆盖 X 条任务，持续保持 X% 通过率。

没有真实数据时使用“当前验证范围”而不是虚构线上数据。

## 十三、面试准备输出

每个阶段完成后，同步维护：

1. 一页架构说明。
2. 一页评测与指标说明。
3. 三个真实故障案例。
4. 五个关键设计决策及备选方案。
5. 十个高频面试追问。
6. 项目局限性和下一阶段规划。

重点准备这些社招问题：

- 为什么需要 Agent，而不是普通 RAG？
- 如何避免 Agent 无休止循环和成本失控？
- 如何评测工具选择是否正确？
- 如何防止模型越权执行副作用？
- 如何处理幻觉、冲突资料和过期知识？
- 如何保证多租户隔离和审计？
- 如何定位一次多步执行失败？
- 如何在真实系统中灰度、回滚和恢复？
- 你做过最难的性能和稳定性问题是什么？
- 如果流量增长 100 倍，架构如何演进？

## 十四、总体验收标准

只有同时满足以下条件，项目才算达到社招展示标准：

1. Agent 主流程有明确状态、工具、重试、人工接管和失败边界。
2. 150 条以上 Golden Set 可重复运行。
3. 任务、工具、引用、转人工、延迟和成本指标均有报告。
4. 每次 Run 可追踪到节点、工具、模型、Token、成本和结果。
5. 外部副作用操作具备权限、dry-run、幂等和审批。
6. 测试、CI、Docker、部署和回滚流程完整。
7. 有在线 Demo 或一条命令可复现的本地 Demo。
8. 有架构图、评测报告、演示脚本和真实故障复盘。
9. 所有简历指标都能在项目中复现。
10. 能围绕项目连续回答 30 分钟以上的技术追问。

## 十五、输出格式

每次执行后，按以下格式汇报：

1. 本阶段目标。
2. 已完成内容。
3. 改动文件和接口。
4. 测试与评测结果。
5. 新旧指标对比。
6. 风险和未解决问题。
7. 对简历和面试表达的影响。
8. 下一阶段建议。

第一次执行只完成“现状审计”，不要直接修改业务代码。
