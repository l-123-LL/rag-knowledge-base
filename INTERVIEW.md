# 面试用项目总结（INTERVIEW）

> 用途：面试时的统一素材来源。**所有数字都是本机实测的，不是估算**，第 14 节逐项标注了口径和局限，被追问时照实说。
> 相关文档：`PROGRESS.md`（当前进度）、`HANDOFF.md`（架构与决策）、`AGENTS.md`（约定）。

---

## 1. 怎么用这份文档

| 场景 | 看哪节 |
| --- | --- |
| 30 秒电梯陈述 | 第 2 节 |
| 简历条目（直接粘贴） | 第 3 节 |
| 3 分钟自我介绍 | 第 4 节 |
| 被追问架构和选型 | 第 5、6 节 |
| 被追问数据、切分、Prompt | 第 7、8 节 |
| 被追问评估指标 | 第 9 节 |
| 被追问工程化和上线能力 | 第 10 节 |
| 被问「最难的问题怎么解决的」 | 第 11 节（四个真实故事） |
| 被问「项目有什么不足」 | 第 12 节 |
| 常见追问速答 | 第 13 节 |
| 数字口径、复现命令 | 第 14 节 |

---

## 2. 30 秒电梯陈述

> 我做的是一个企业智能客服 RAG 系统。企业把产品手册、FAQ、售后和物流政策导进知识库，客户提问时，系统先做 FAQ 精确命中，没命中再走「BGE 中文向量 + BM25」混合检索，把命中的原文片段和问题一起交给 DeepSeek 生成带来源引用的客服回答；资料里找不到答案时明确拒答并引导转人工。检索侧我用自建的 50 条客服问题集做评估，实测 hit@1 是 0.96、hit@3 是 1.00、MRR 是 0.98；端到端流式回答首 token 约 2.5 秒，单次查询约 187 个 token；后端 54 个测试、前端 18 个测试全部通过，并且接入了 GitHub Actions CI。

---

## 3. 简历条目（可直接粘贴）

1. **独立设计并实现企业智能客服 RAG 系统**（Python FastAPI + React 18 / TypeScript，前后端约 3900 行代码，每次改动一个 commit、累计 60 多次可回溯提交）：覆盖资料导入（TXT / Markdown / HTML / 文本层 PDF / 网页 URL）、清洗切分、索引构建、混合检索、流式生成、引用溯源、反馈与工单全链路。

2. **构建「向量 + 关键词」混合检索**：本地部署 `BAAI/bge-large-zh-v1.5`（1024 维、归一化内积）配合 FAISS 索引，叠加 jieba 分词 + `rank-bm25` 关键词召回，按权重融合后取 top-k；自建 50 条中文客服问题评估集实测 **hit@1 = 0.96、hit@3 = 1.00、MRR = 0.98**，同规模下测试替身基线 hit@1 为 0.90。

3. **设计三层应答策略降低幻觉**：标准 FAQ 精确命中直接返回标准答案（零延迟、零调用成本）→ 未命中走 RAG 生成并要求引用原文 → 检索为空或最高余弦相似度低于阈值时判定资料不足，自动建工单并返回带工单号的转人工话术；投诉和明确的转人工请求由规则意图路由接管（带否定词防误判，「我不想转人工」不会触发）。阈值在本机语料上校准：相关问题最低 0.374、无关问题最高 0.401，取 0.42 可完全分开。

4. **量化性能与成本**：实测单次检索（含查询向量化，CPU）平均 74 ms、P95 80 ms；DeepSeek 流式回答首 token 2.5–3.0 s；单次查询消耗约 187 tokens（输入 153 / 输出 34）；问答日志逐条记录路由、模型、延迟、token 与成本，`/metrics` 可直接聚合。

5. **工程化与质量保障**：65 个后端 pytest + 19 个前端 Vitest 用例全绿，GitHub Actions 在每次提交自动跑测试、类型检查与构建；实现 SSE 流式输出与降级回退、会话记忆持久化、多租户数据隔离（`X-Tenant-ID`）、管理员鉴权与限流、可选 OIDC/SSO 与 OCR 钩子、FAQ 运行时热更新、Docker Compose 部署。

---

## 4. 3 分钟自我介绍口述版

**背景与目标。** 企业客服有个典型痛点：客户问的问题大部分能在已有资料里找到答案，但坐席要在手册、FAQ、政策文档之间来回翻；直接让大模型回答又会编造政策、乱承诺。所以我做了一套以「可溯源」为第一原则的客服问答系统，目标是能答的标准问题快速答，不能答的明确说不确定并转人工。

**整体流程。** 企业资料通过上传文件、网页 URL 或直接调 `/ingest` 进入系统，经过清洗和切分后写入两个索引：一个是本地 BGE 中文向量加 FAISS，一个是 jieba 分词加 BM25 的关键词索引。用户提问时先过 FAQ 精确匹配和规则意图路由，没命中就做混合检索，把 top-k 原文片段连同问题送进 DeepSeek 生成答案，返回时带上每条引用的来源和命中片段，前端可以逐条展开看原文。

**为什么这么选。** 嵌入放本地，中文效果好、没有按次计费、企业资料不出内网；生成放云端，是因为本地跑生成模型对这台机器不划算。向量库选 FAISS 而不是 Chroma、pgvector、Milvus，是因为当前是单机中小规模场景，多一个服务进程纯属负担，但我把向量库抽成了 `VectorStore` 接口，换实现不用动检索和管线。检索做成混合，是因为客服问题里有大量专有名词和条款名，纯向量对精确词面匹配不稳定。

**效果与工程化。** 检索侧我建了 50 条客服问题评估集，实测 hit@1 是 0.96、hit@3 是 1.00、MRR 是 0.98；端到端首 token 大约 2.5 秒，单次约 187 个 token。工程上做了 SSE 流式输出、会话记忆持久化、多租户隔离、可选鉴权和限流、结构化日志和监控 Webhook、备份、Docker 和 CI，测试是 54 个后端加 18 个前端用例全绿。

**我踩过最深的一个坑**是评估指标的口径：最初的评估脚本为了跑得快，默认用的是测试替身 `HashEmbedder`，指标看起来不错但那不是线上链路；我发现之后换成真实 BGE 复测，数字从 0.90 变成 0.96，同时把「评估必须走线上同一条链路」写进了项目规则。

---

## 5. 系统架构与数据流

模块划分图见 `docs/architecture-modules.svg`（矢量）和 `docs/architecture-modules.png`（位图），文字与 Mermaid 版见 `docs/architecture-modules.md`。整张图按六层组织：前端 → 接口 → 编排 → 检索与生成 → 存储 → 横切能力。

### 数据流（导入侧）

```text
企业资料（PDF / Markdown / HTML / TXT / 网页 URL）
  -> 解析与清洗（pypdf、pdfplumber 表格转 Markdown、正文抽取）
  -> 切分（段落聚合 + 窗口切分，块大小与重叠可配置）
  -> 嵌入（BAAI/bge-large-zh-v1.5，1024 维，归一化）
  -> FAISS 索引（IndexFlatIP）+ 元数据 JSON，落盘可重启恢复
  -> BM25 索引（jieba 分词）
```

### 数据流（问答侧）

```text
用户提问
  -> FAQ 精确命中？ 是 -> 直接返回标准答案（不调用大模型）
  -> 否
  -> 规则意图路由（投诉 / 转人工）命中 -> 客服话术 + 自动建工单
  -> 否
  -> 混合检索：向量 top-k 与 BM25 top-k，归一化加权融合（dense 权重 0.7）
  -> 可选 rerank（BGE reranker，默认关闭）
  -> 相关性足够？ 否 -> 拒答：资料不足 + 建议转人工
  -> 是
  -> DeepSeek 生成（system prompt 约束只依据资料、要求引用来源）
  -> SSE 流式返回 + 结构化引用（来源、页码 / 章节、命中片段）
  -> 日志 / 指标 / 反馈 / 工单 / 告警
```

### 模块划分

| 层 | 关键模块 | 职责 |
| --- | --- | --- |
| 接口层 | `backend/app/main.py`、`schemas.py` | 20 个 REST 接口、SSE 流式、鉴权、限流 |
| 管线层 | `pipeline.py`、`factory.py` | 串联检索与生成，依赖注入便于测试替换 |
| 检索层 | `retrieval.py`、`vector_store.py`、`embeddings.py`、`reranker.py`、`chunking.py` | 切分、嵌入、向量库、BM25、融合、重排 |
| 生成层 | `generation.py` | DeepSeek 客户端，普通与流式两种调用 |
| 业务层 | `faq_store.py`、`intent.py`、`ticket_store.py`、`session_store.py`、`tenant.py` | FAQ 热更新、意图路由、工单、会话记忆、多租户 |
| 运维层 | `observability.py`、`cost.py`、`backup.py`、`security.py` | 结构化日志、成本估算、备份、鉴权 |
| 评估层 | `backend/evaluation/`、`app/judge.py` | 检索指标、生成质量打分 |
| 前端 | `web/src/` | 企业客服工作台、流式对话、引用展开、知识库管理（管理员视图） |

---

## 6. 关键技术决策与理由

| 决策点 | 选择 | 考虑过的备选 | 为什么这么选 |
| --- | --- | --- | --- |
| 嵌入模型 | `BAAI/bge-large-zh-v1.5`（1024 维，归一化，内积） | OpenAI text-embedding、m3e、bge-small | 中文检索效果好、本地推理无按次费用、企业资料不出内网；large 比 small 精度更高，本机 CPU 单条编码约 142 ms 可接受 |
| 向量库 | FAISS（`IndexFlatIP` + 元数据 JSON） | Chroma、pgvector、Milvus | 单机中小规模无需额外服务进程；精度优先选 Flat 精确检索；已抽 `VectorStore` 接口，换 pgvector/Milvus 只加实现不改调用方 |
| 关键词检索 | jieba + `rank-bm25` | 单字切分、Elasticsearch | 药名、型号、条款名等专有名词需要词面匹配；单字切分噪声大；ES 对当前规模太重 |
| 融合策略 | 归一化后加权求和，dense 权重 0.7 | RRF、纯向量 | 两路分数量纲不同，先 min-max 归一再融合；权重可调，保留后续换 RRF 的空间 |
| top-k | 默认 5，可传参；rerank 候选 20 | 3 / 10 | 客服答案通常集中在 1 到 2 个片段，5 条兼顾召回与 prompt 长度；rerank 前放大到 20 条保证候选池 |
| rerank | 可选开关（`RERANK_MODEL`） | 默认开启 | 首次加载要多下载几百 MB 权重并增加延迟，默认关闭保证开箱能跑，需要精度时再开 |
| 生成模型 | DeepSeek `deepseek-chat` | 本地 Qwen、GPT 系列 | 中文客服语气好、成本低、OpenAI 兼容协议接入简单；本地部署生成模型对这台机器不划算 |
| 后端框架 | FastAPI + Uvicorn | Flask、Django | 原生异步适合流式、自带 OpenAPI 文档、与 pytest 和 httpx 集成成熟 |
| 前端 | React 18 + Vite + TypeScript + Tailwind | Vue、Next.js | 后台工作台场景不需要 SSR；TS 让接口对接错误在编译期暴露；Tailwind 保证视觉一致 |
| 切分粒度 | 段落聚合 + 窗口切分，块大小与重叠可配置 | 固定长度硬切 | 硬切会把语义切断，导致检索到半句话；按段落优先能保住语义边界 |
| 拒答策略 | 检索不足直接拒答 | 让模型兜底 | 客服场景编造政策或承诺的代价远高于说一句「我帮您转人工」 |

---

## 7. 数据与切分

**数据来源。** 当前索引里的语料是自建示例资料（退货、物流、发票、转人工、地址修改、优惠券、账号找回等客服主题），真实企业资料尚未导入——这一点面试时不要含糊，我准备的第一批资料是产品手册、FAQ 表格、售后与物流政策。

**支持格式。** TXT、Markdown、HTML、文本层 PDF（`pypdf` + `pdfplumber` 抽表格转 Markdown 后参与索引）、公开网页 URL（正文抽取）。扫描版 PDF 走可选 OCR 钩子（`OCR_ENABLED=true`），未实测。

**清洗与乱码。** HTML 去标签与正文抽取，多余空白和换行归一化；PDF 逐页取文本层再合并；文本统一按 UTF-8 处理，遇到非 UTF-8 内容按声明编码回退。乱码和表格错位是后续要补的工程点，目前只验证了文本层 PDF。

**切分规则。** 先按标题和段落边界聚合，再对超长段落做窗口切分；默认块大小 600 字符、重叠 80 字符（`backend/app/chunking.py` 里的 `DEFAULT_CHUNK_SIZE` / `DEFAULT_OVERLAP`，可配置）。每个 chunk 带 `source`、`chunk_index`、`tenant_id`、`id` 元数据，检索结果靠这些元数据回溯原文位置。重叠的目的很直接：关键条件句（例如「定制类商品除外」）跨边界时，前后两个块都能命中。

---

## 8. Prompt 与防幻觉设计

**系统提示词**（当前实现在 `backend/app/generation.py`）：

```text
你是企业智能客服助手。只能根据提供的知识库资料回答，不得编造。
如果资料不足，请说明无法确认，并引导用户联系人工客服。
回答要简洁、专业、友好，并尽量引用来源。
```

**上下文拼装。** 每个检索片段按「来源标记 + 原文」拼进 user prompt，让模型有条件引用；`temperature` 设为 0.2 降低发散；会话记忆作为历史消息一并传入，保证多轮指代能接上。

**引用怎么保证。** 回答里的引用不靠模型自己写，而是由后端把本次检索命中的片段结构化成 `citations` 字段（来源、页码或章节、命中片段）随答案一起返回，前端逐条折叠展示。模型即使漏写引用，前端引用面板依然真实可查——这比要求模型自己保证引用更可靠。

**检索不到怎么拒答、怎么转人工。** 一共三层：

1. `RAGPipeline` 在检索结果为空，或最高原始余弦相似度低于 `RAG_MIN_SCORE` 时，直接判定「资料不足」，不调用模型。这里必须用归一化前的原始分数：归一化分数永远有最大值 1.0，做不了绝对判断。
2. 接口层统一走 `escalate_to_human`：建工单、把工单号拼进话术，`/ask` 与 `/ask/stream` 行为一致，日志里记 `status=insufficient` 和 `escalated=true`。
3. 提示词再兜一层：明确要求「资料不足就说明无法确认，并引导用户联系人工客服」。

前端在输入框旁边固定放了一个「转人工」按钮，用户不用自己想措辞；投诉、明确要求转人工、资料不足这三种情况都会产出一张工单，管理员可以用 `/tickets` 查，也可以配 Webhook 推到 CRM。

---

## 9. 评估方法与实测指标

### 检索评估（自建 50 条客服问题集）

评估集定义在 `backend/evaluation/enterprise_eval.py`：10 个客服主题，每个主题 5 种问法，共 50 条问题；每个主题配一小段标准资料，判定标准是命中的 chunk 是否属于该主题。

| 指标 | 真实 BGE 模型（本次实测） | 测试替身 HashEmbedder（旧口径） |
| --- | --- | --- |
| hit@1 | **0.96** | 0.90 |
| hit@3 | **1.00** | 0.98 |
| hit@5 | **1.00** | 1.00 |
| MRR | **0.98** | 0.945 |

hit@k 指前 k 条结果里包含正确来源的比例；MRR 指第一条正确结果排名倒数的均值。两个口径的差距本身就是个面试点，见第 11 节故事一。

### 端到端延迟与用量（真实 DeepSeek 调用，3 个问题实测）

| 指标 | 实测值 |
| --- | --- |
| 检索耗时（含查询向量化，CPU） | 80.6 / 81.6 / 95.6 ms |
| 首 token 延迟（SSE 流式） | 2510 / 2510 / 2967 ms |
| 流式回答总耗时 | 2642 / 2689 / 3128 ms（回答 28 到 52 字） |
| 非流式单次调用 | 5945 ms |
| 单次查询 token | 输入 153 + 输出 34 = **187** |

按每次 187 tokens 折算：每 1000 次查询约消耗 0.153M 输入 tokens 加 0.034M 输出 tokens，乘以在 `backend/.env` 配置的单价即可得到成本。工程里已经实现了 `calculate_cost`，只是单价目前留空，所以 `/metrics` 里的成本字段还是空的。

### 生成质量评估

实现了 RAGAS 风格的 faithfulness 与 relevance 两维打分（`backend/app/judge.py` 配合 `backend/evaluation/generation_eval.py`），可用大模型当裁判批量跑；目前还没有在正式测试集上跑出报告，属于待补项。

### 自动化测试

| 项目 | 数量 | 结果 |
| --- | --- | --- |
| 后端 pytest | 103 | 全部通过 |
| 前端 Vitest | 22（7 个文件） | 全部通过 |
| 类型检查与构建 | `tsc --noEmit` 与 `vite build` | 通过，已接入 CI |

---

## 10. 工程化能力清单

- **流式输出**：`POST /ask/stream` 用 SSE 逐段返回，前端逐字渲染；不支持流式的客户端自动降级到普通请求。
- **会话记忆**：前端自动生成 `session_id`，后端把最近对话持久化到 `backend/data/sessions/`，服务重启后上下文不丢。
- **多租户隔离**：`X-Tenant-ID` 贯穿来源、FAQ、会话、工单和检索元数据，租户之间互不可见。
- **安全与配额**：可选管理员 API Key、每分钟限流、可选 OIDC/JWKS 的 Bearer Token 校验。
- **可观测**：每条问答写 JSONL（路由、模型、延迟、token、成本、引用数），`/metrics` 聚合查询量、平均延迟、token、成本与有帮助率，`/alerts` 按延迟和低有帮助率告警，可选 Webhook 外发到 Langfuse 类平台。
- **知识库运维**：资料可停用与恢复，停用即从检索中排除；FAQ 支持运行时增删改（带版本号），普通用户前端看不到管理入口。
- **工单闭环与转人工**：规则意图（投诉、明确要求转人工）+ 资料不足兜底都会自动建单，返回带工单号的话术；`/tickets` 可查，可 Webhook 外发到 CRM；前端提供一键「转人工」入口，普通用户也能用。
- **备份**：管理员接口把数据目录打包成备份包。
- **部署**：前后端各一个 Dockerfile 加 `docker-compose.yml`，`/api` 由 Nginx 反代到后端。
- **CI**：GitHub Actions 在 push 或 PR 时跑后端测试、前端测试、类型检查和构建。

---

## 11. 踩坑与排查（面试官最爱，讲这四个）

### 故事一：评估指标口径不对，指标虚高，换成真实链路后重测

**现象。** 50 条评估集第一次跑出来 hit@1 = 0.90、MRR = 0.945，看着不错，准备写进材料。

**排查。** 复盘「这个数字能不能代表线上」时我去看评估代码，发现 `run_retrieval_evaluation` 为了跑得快、不依赖模型下载，默认注入的是测试替身 `HashEmbedder`（把词哈希成 64 维向量），而线上链路用的是 1024 维的 `bge-large-zh-v1.5`。也就是说，这个指标衡量的是替身，不是产品。

**动作。** 用真实 BGE 重跑同一套 50 条问题，得到 hit@1 = 0.96、hit@3 = 1.00、MRR = 0.98，同时记录单次检索平均 74 ms、P95 80 ms。旧口径反而更差，因为哈希向量只有 64 维，语义泛化能力弱。

**收获。** 评估必须走线上同一条链路，替身只允许出现在单元测试里。这条经验已经写进项目约定。

### 故事二：检索延迟随索引规模非线性增长，定位到 O(N²) 热点

**现象。** 10 条语料的索引上检索 P95 是 80 ms；把语料扩大到 512 条 chunk 后，检索平均涨到 370.9 ms、P95 涨到 444.3 ms，涨幅远超「数据量变大」该有的程度。

**排查。** 拆热点：查询向量化约 74 ms 是固定成本，剩下的时间在 BM25。看代码发现 `BM25Index.score(query, i)` 内部每次都调用一次 `get_scores()` 做全量打分，而 `HybridRetriever.search` 是按文档索引逐个调用它，等于对每个候选文档都重算一遍全部文档的分数，复杂度是 O(N²)。

**验证。** 单独做微基准：512 条文档下循环调用共 88.3 ms，一次性打分只要 0.38 ms，相差约 233 倍。

**状态。** 已修复：新增 `BM25Index.scores()` 一次性打分，检索层改为单次调用，同时保留原 `score()` 签名以兼容既有调用。512 条索引下检索从平均 370.9 ms / P95 444.3 ms 降到 **187.5 ms / 193.4 ms**，并且补了一条"批量打分结果必须与逐文档打分一致"的回归测试。

### 故事三：拒答边界，不能编造

**现象。** 早期版本遇到「今天天气如何」这类知识库外的问题会顺着上下文硬答。改成拒答之后，更隐蔽的问题在复盘材料时暴露出来：文档里写着「检索不到会引导转人工」，但代码里其实只有命中关键词才转人工，检索为空只回一句「暂时无法给出可靠回答」，既不建单也没有转人工出口；同时「怎么联系人工客服？」被意图规则拦死，永远命中不了 FAQ 里那条服务时间。

**排查。** 用真实接口跑四类问句，把每条的实际路由和返回都打出来，问题一次暴露：资料不足没有出口、FAQ 成了死条目、以及「我不想转人工」也会被强行转接。

**动作。** 四件事一起改：

1. 抽统一出口 `escalate_to_human`：意图命中、投诉、检索为空、相似度低于阈值这四种情况都建工单并返回带工单号的话术，`/ask` 与 `/ask/stream` 行为一致。
2. 阈值只能用原始余弦相似度判断（归一化分数永远有最大值 1.0），于是给检索结果补了 `raw_dense_score` 字段，并新增 `RAG_MIN_SCORE` 配置。
3. 用真实模型采样校准：50 条相关问题最高余弦相似度最低 0.374，8 条无关问题最高 0.401，取 0.42 可以把两组完全分开。
4. 收紧意图关键词并加否定词防误判，把「人工客服」这类描述性说法放回 FAQ，死条目复活。

**收获。** 文档里写的功能必须能被代码验证。我现在的要求是：每条对外讲的能力都要能用一条命令或一个测试跑出来，跑不出来的就从材料里删掉。

### 故事四：模型与依赖的离线化

**现象。** 本机网络受限，`SentenceTransformer` 加载模型时会去访问 HuggingFace 校验文件，触发多轮重试，直接把启动时间拖到几分钟。

**动作。** 通过 `HF_ENDPOINT` 配国内镜像、`HF_HOME` 把权重缓存到项目内 `models/` 目录，并支持离线加载；缓存和索引都加进 `.gitignore`，既避免提交大文件，也保证换机器时按文档重新拉取即可。

---

## 12. 项目短板（被问「哪里做得不好」就照这个说）

- **语料还是示例级。** 真实企业资料没有导入，检索指标来自 10 条短文本的评估集，属于乐观区间；换成真实长文档后指标一定会下降，届时要重测。
- **评估集规模小，生成质量没有正式报告。** 50 条问题、每主题一小段文本；faithfulness 与 relevance 只实现了打分能力，没跑出批量报告。
- **成本与延迟只测了小样本。** 3 个真实问题的 TTFT 和每次 187 tokens，不能代表长上下文场景；单价未配置，`/metrics` 成本字段为空。
- **rerank 与 OCR 是「有代码没实测」。** 属于工程上的半成品，不能算已完成能力。
- **上线能力只是钩子。** 鉴权、限流、OIDC、Webhook、Docker 都写好了，但默认关闭且没做过部署验证；没有远程 Git 仓库，也没有真实并发压测。
- **已知性能问题。** 第 11 节故事二里的 BM25 O(N²) 尚未修复。
- **缺少前端移动端自动化回归，也没有 lint 与 format 脚本。**

---

## 13. 高频追问速答

**Q：为什么需要 Agent，而不是普通 RAG？**
A：普通 RAG 只能回答"资料里写过什么"，答不了"我的订单现在到哪了"这类实时、结构化、需要跨系统操作的问题；它也没有失败恢复机制——工具挂了、资料过期、用户要求退款，RAG 只能硬答或者不答。所以我把系统做成两条链路：默认 RAG 不变，工具模式跑状态机（路由 → 工具 → 质量判断 → 回答或转人工），并把"答不出来"变成有出口的流程（建单转人工）。数据能说明这个取舍的价值：150 条评测里 **86% 的请求根本不走模型**（FAQ 41、订单工具 35、转人工 28、物流工具 25），真正需要生成的只有 21 条——Agent 的价值不是"多调模型"，而是把不该由模型做的事交回给规则和工具，同时让该拒绝的时候有地方可去。

**Q：为什么不用 LangChain 或 LlamaIndex？**
A：这个项目的核心链路（切分、嵌入、两路检索、融合、生成）代码量不大，用原生实现我能完全控制融合权重、拒答阈值和元数据结构，也更好做单元测试；框架抽象在这些细节上反而会挡住我，而且依赖升级容易带来不可控变化。如果要做多数据源、多 Agent 编排，我会考虑引入。

**Q：切分大小怎么定的？**
A：中文检索里块太大容易引入噪声，太小会切断条件句。我按段落优先聚合，超长再窗口切分，块大小和重叠做成可配置参数，默认 600 字符、重叠 80 字符，目的是让跨边界的关键条件（比如「定制类商品除外」）在相邻块里都能命中。

**Q：top-k 为什么是 5？**
A：客服答案通常集中在 1 到 2 个片段，5 条能在召回和 prompt 长度之间取平衡；接 rerank 时会把候选先放大到 20 条再精排，避免召回阶段就丢掉正确答案。

**Q：怎么防止模型编造？**
A：四层。第一层 FAQ 精确命中直接给标准答案，不经过模型；第二层检索为空、或最高原始余弦相似度低于 `RAG_MIN_SCORE` 时判定资料不足，不调用模型；第三层资料不足会自动建工单并返回带工单号的转人工话术，用户不会卡在一句「不知道」上；第四层提示词强约束「只能依据资料、不足就说明无法确认并转人工」，并要求引用。引用本身由后端从检索结果结构化生成，不依赖模型自觉。

**Q：转人工具体怎么实现的？**
A：两条入口。一条是规则意图：问题里出现「转人工」这类明确请求，或者出现「投诉」，规则直接判定，跳过检索和生成，不让模型在政策承诺上有发挥空间（带否定词防误判，「我不想转人工」会正常走问答）。另一条是兜底：检索为空或相似度低于阈值时同样转人工。两条入口都走同一个 `escalate_to_human`，建一张工单（落盘 JSON，可 Webhook 推到 CRM），把工单号拼进回复，`/ask` 和 `/ask/stream` 行为一致，前端输入框旁边有固定的「转人工」按钮。已知边界：目前只有登记和话术，没有坐席排队、分配和实时会话。

**Q：怎么防止模型越权执行副作用操作？**
A：分三层。第一层是工具分级：低风险写操作（本地建工单）直接执行，高风险写操作（退款、改 CRM）必须走审批。第二层是 dry-run：`execute_tool(dry_run=True)` 只回放将要发送的参数、不产生任何写入，用来给审批人看预览。第三层是审批与幂等：未批准时只生成审批单（`GET /approvals` 可见、`POST /approvals/{id}/decision` 批准），批准后才真正调用工具；审批带幂等键（租户+工具+参数），重复批准不会重复执行，重复提交也不会重复建单。当前 mock 的「退款执行」只建单记录、不动真实资金，接真实系统时替换 handler 即可，审批与幂等逻辑不变。

**Q：资料过期、新旧政策冲突怎么处理？**
A：三件事一起做。第一，知识带版本与生效时间：`/ingest` 写 `doc_key` / `version` / `effective_from` / `effective_to`，检索时同一 `doc_key` 只保留版本号最高的片段，避免新旧政策同时命中；第二，`/ask` 支持 `as_of`，可以回答"去年 8 月时政策是怎么规定的"，默认按今天；第三，冲突无法用规则判断时（比如两份资料说法不一致），检索结果里同时出现而模型无法取舍，就按"资料不足"拒答并转人工——宁可转人工也不猜。实测代价：阈值 0.42 会误伤 1/50 相关问题，这条我在文档里写清楚了，没有藏。

**Q：如果流量增长 100 倍，架构怎么演进？**
A：先说现在的硬边界：单实例、CPU 推理、状态在本地文件（FAISS 索引、FAQ、会话、限流计数），实测不调模型的路径吞吐约 180 RPS 就饱和，检索单请求 74 ms、生成首 token 2.5–3 s。100 倍意味着三层都要拆：① 无状态化——会话、限流、FAQ、工单从本地文件挪到 Redis 与 Postgres，向量库换 pgvector 或 Milvus，让 API 能水平扩多副本（代码里 `VectorStore` 接口和依赖注入工厂就是为这个留的口子）；② 推理分离——嵌入从进程内改成独立服务（GPU 或 ONNX 加速），生成侧加请求合并与熔断；③ 入口层——网关限流、按租户配额、灰度分流。可观测性也要跟着换：从本地 JSONL 换成集中式（Langfuse / ClickHouse），否则多副本下查不到完整链路。

**Q：怎么做灰度、回滚和恢复？**
A：三层。① 代码层：每次改动一个 commit（当前 100+ 个），`docs/DEPLOYMENT.md` 里写了回滚命令，只回代码不动数据；② 配置层：`workflow_mode` 让新链路作为可选模式上线、默认走老链路，出问题一个参数回退，`RAG_MIN_SCORE`、`RERANK_MODEL`、`ADMIN_API_KEY` 全是环境变量；③ 数据层：`data/` 是宿主机 bind mount，回滚代码不丢数据，`POST /backup` 可打包整个数据目录。故障恢复实测过：杀掉后端后调用方 2.1 秒快速失败（不挂死），前端页面照常打开（200/17 ms），容器重启 5 秒内恢复健康。诚实边界：目前还是单实例演示项目，没有真正的按比例切流，这一步要等引入网关之后做。

**Q：混合检索的权重怎么定的？**
A：向量 0.7、BM25 0.3，两路分数先做 min-max 归一化再加权，因为量纲不同不能直接相加。权重目前是经验值，属于需要用评估集扫参的项；50 条集上 hit@3 已经到 1.00，再往上调收益不明显，所以我把它列为待优化项。

**Q：怎么评估效果好不好？**
A：检索侧用 hit@1、hit@3、hit@5 和 MRR，自建 50 条客服问题集，实测 hit@1 是 0.96、hit@3 是 1.00、MRR 是 0.98。生成侧实现了 RAGAS 风格的 faithfulness 与 relevance 打分，还没跑正式报告。线上还有「有帮助或没帮助」反馈和转人工率可以当业务指标。

**Q：延迟瓶颈在哪？**
A：端到端两段。检索在 CPU 上 74 ms 左右，主要是查询向量化；大头是生成的首 token，约 2.5 秒。要压延迟可以换更小的嵌入模型、把嵌入服务 GPU 化，或者用流式先把「正在查询」反馈给用户。

**Q：向量库为什么不用 Milvus 或 pgvector？**
A：当前是单机中小规模，FAISS 不需要额外服务进程，精确检索（`IndexFlatIP`）召回最稳。数据量到百万级、要在线增删和高可用时，我会换 pgvector（如果本来就有 Postgres）或 Milvus（要独立部署和运维）。代码里已经抽出 `VectorStore` 接口，换实现不影响检索和管线。

**Q：PDF 里的表格和扫描件怎么办？**
A：文本层 PDF 已经支持，表格用 pdfplumber 抽出来转成 Markdown 参与索引；扫描件走可选 OCR 钩子（`OCR_ENABLED`），但目前没实测，属于待验证能力，我不会说它已经支持。

**Q：怎么做多租户隔离？**
A：请求头 `X-Tenant-ID` 贯穿来源、FAQ、会话、工单和检索元数据，检索时按租户过滤，不同租户资料互相不可见。当前是逻辑隔离（同一进程、同一份存储目录下按租户分目录）；如果要做强隔离，下一步是把索引和存储按租户物理拆分。

**Q：有并发和缓存设计吗？**
A：目前是单进程同步管线，量化数据是单请求串行测出来的，没有做并发压测，也没有 Redis 缓存层，这是明确的短板。可以做的方向是嵌入批量化、FAQ 命中结果缓存、生成侧请求合并，以及多 worker 加队列承载并发。

**Q：这个项目最大的技术难点是什么？**
A：不是调模型，而是让系统的回答可信。为此我把工程重点放在三处：检索必须能召回正确原文（混合检索加评估集量化）、回答必须能溯源（结构化引用）、答不出来必须承认（两层拒答）。其次是性能，在扩大语料时发现检索延迟非线性增长，并定位到 BM25 的 O(N²) 热点。

**Q：如果重做你会改什么？**
A：三件事。第一，先定评估集再做实现，把「指标口径和线上链路一致」固化到 CI 里；第二，一开始就把向量库和嵌入抽象成接口，这次是中途补的，但补得及时；第三，早点做并发和缓存，因为延迟瓶颈最后不在检索，而在生成和串行处理。

---

## 14. 数字口径与复现命令

**本机环境**：Windows，Intel 12 代平台 12 逻辑核，Python 3.12.14，Node 24.15.0；嵌入与检索全部 CPU 推理。

### 检索指标（真实 BGE）

命令行现在默认走真实模型，直接跑下面这条就是线上口径（`--embedder hash` 才是快速回归用的替身口径，hit@1 = 0.90）：

```bash
cd backend
..\.venv\Scripts\python.exe -m evaluation.enterprise_eval --offline
```

### 测试

```bash
cd backend
..\.venv\Scripts\python.exe -m pytest -p no:cacheprovider -q   # 119 passed

cd web
npm test                                                      # 6 files / 19 tests
npm run typecheck
npm run build
```

### 转人工阈值校准

用真实 BGE 在同一套 10 条示例语料上采样两组分数（取每条问题检索结果的最高原始余弦相似度）：

| 分组 | 样本数 | 最小值 | 中位数 | 最大值 |
| --- | --- | --- | --- | --- |
| 相关问题（50 条客服问题） | 50 | 0.374 | 0.706 | 0.812 |
| 无关问题（天气、股票、做饭、编程等） | 8 | 0.197 | 0.323 | 0.401 |

实测取舍（0.42）：能挡下 8/8 无关问题，但会误伤 1 条相关问题（保留 49/50）；放宽到 0.38 则保留 50/50、只能挡 7/8。当前选 0.42，属于"宁可多转人工、不硬答"的取向。该值写在本地 `backend/.env`（不提交），`backend/.env.example` 里默认是 0（关闭），换真实资料必须重新采样校准。

### Agent 任务评测基线（150 条，零 API 花费）

命令行：`python -m evaluation.agent_eval --tag full --use-real-embedder --offline`（约 18 秒跑完）。评测集 = 100 条新增 Agent 任务（订单 25 / 物流 15 / 政策 15 / 多轮 20 / 投诉转人工 13 / 拒答与注入 12）+ 50 条知识问答。默认用确定性生成器，所以测的是路由、工具、参数、转人工与降级这些**不依赖生成质量**的指标。

| 指标 | 数值 |
| --- | --- |
| 任务成功率 | 99.33% |
| 路由 / 工具选择准确率 | 100.00% |
| 工具参数准确率 | 100.00% |
| 引用准确率 | 87.32% |
| 违规 / 幻觉率 | 0.00% |
| 转人工 Precision / Recall | 96.43% / 100.00% |
| 自动解决率 | 99.19% |
| P50 / P95 延迟 | 1 ms / 102 ms |

失败样本诚实说：`积分有什么用？` 在阈值 0.42 下被判资料不足并转人工，属于阈值取舍的代价；知识类问题的严格文案命中率只有 60%，因为答案是 FAQ 的措辞而不是语料原文。

### 并发压测基线（单实例，2026-09-19 实测）

脚本 `backend/evaluation/load_test.py`，打的是不依赖外部模型的路径（订单 / FAQ / 转人工），所以零 API 花费、可反复跑。

| 并发 | 成功 | P95 延迟 | 吞吐 |
| --- | --- | --- | --- |
| 1 | 120/120 | 52.6 ms | 24.9 RPS |
| 10 | 120/120 | 81.9 ms | 177.0 RPS |
| 20 | 120/120 | 125.6 ms | 182.0 RPS |
| 40 | 120/120 | 210.4 ms | 183.8 RPS |
| 80 | 120/120 | 350.8 ms | 164.0 RPS |

600 次请求零错误；吞吐在并发 20 左右饱和（约 180 RPS）。默认限流 120 次/分钟/IP 会先于容量上限触发（并发 20、40 次请求时全部 429），这是设计行为。

口径边界：知识问答要等 DeepSeek（首 token 2.5–3 s），CPU 版 BGE 检索吞吐约 13–16 QPS —— 真正决定用户体验的是这两个数字，上面的 180 RPS 只代表"不调模型的业务路径"。

### 成本实测（真实模型，2026-09-19）

按 deepseek-flash 空闲时段官方单价配置后跑 60 条核心集：60 条里只有 4 条走了模型（其余是 FAQ / 订单工具 / 转人工，零成本），平均每次调用 230 token、**$0.000058**（约 ¥0.0004），60 条总计 $0.000234。

外推到真实语料（5 条 600 字 chunk ≈ 2,800 输入 + 250 输出）：单次约 **$0.00057（¥0.004）**，每 1000 次问答约 **$0.57 / ¥4**。标注清楚：这是外推值，实测只在示例语料上做过；换成真实企业资料后必须重测。这套口径也解释了"为什么 FAQ 优先"在成本上的价值——FAQ 命中完全不调模型。

### 生成质量（LLM-as-Judge，2026-09-19 实测）

150 条抽样里只有 21 条真正走了模型生成（其余 129 条是 FAQ / 订单工具 / 物流工具 / 规则转人工路径，根本不调模型）。对这 21 条打分：

| 指标 | 数值 |
| --- | --- |
| faithfulness 均值 | **1.00** |
| relevance 均值 | **0.929** |
| 低分条数 | 3（14.29%） |

三条低分全是"优惠券"主题——知识库里没有领取入口与有效期，模型如实回"无法确认，建议联系人工客服"，所以 faithfulness 满分、relevance 半分。这条结论在面试里很好用：**它证明系统在知识不足时选择拒答而不是编造，同时暴露了知识库覆盖的缺口**。

这里有个必须讲清楚的口径：faithfulness 只对"依据资料生成"的答案有意义。第一版报告没做区分，把「投诉转人工」这类规则路径的正确回答也扣成低分，修正后只对生成路径打分，规则路径交给转人工 Precision/Recall 衡量。

### 端到端延迟与 token

需要在 `backend/.env` 配置真实 `DEEPSEEK_API_KEY`，会产生少量调用费用。启动服务后用前端 5173 提问，或直接调用 `POST /ask/stream` 观察流式返回。

```bash
cd backend
..\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

### 这些数字的边界（务必如实说）

- 检索指标基于 10 条短语料的 50 条问题集，属于**自建小型评估集**，不代表真实长文档业务效果。
- 检索延迟 74 ms 是 10 条 chunk 的索引规模；512 条 chunk 时实测平均 370.9 ms、P95 444.3 ms。
- 生成侧 TTFT 与 token 只测了 3 个问题加 1 次非流式调用，prompt 只有 153 tokens，长上下文下会变差。
- 嵌入吞吐实测为 512 条 chunk 共 72.6 秒（约 142 ms/chunk），模型冷加载 11.1 秒，均为 CPU。
- 转人工阈值 0.42 是在 10 条示例语料上采样的（相关 50 条、无关 8 条），样本小且两组间隔只有 0.027，换真实资料必须重新校准。
- 转人工目前只做到「建工单 + 返回话术」，没有坐席排队、分配和实时会话，也没有转人工后的消息回流。
- 没有真实并发、真实用户量和真实企业资料的验证数据。
